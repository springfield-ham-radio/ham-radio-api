default:
  image: node:22
  before_script:
    - corepack enable

stages:
  - build
  - release

cache:
  key: modules
  paths:
    - node_modules/
    - .yarn/cache/
    - dist/

build:
  stage: build
  script:
    - yarn install
    - yarn build

release:
  stage: release
  before_script:
    - apt-get update && apt-get install -y git
  script:
    - yarn release
